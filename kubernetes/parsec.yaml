apiVersion: apps/v1
kind: Deployment
metadata:
  name: parsec
  labels:
    app: parsec
spec:
  replicas: 1
  selector:
    matchLabels:
      app: parsec
  template:
    metadata:
      name: parsec
      labels:
        app: parsec
    spec:
      containers:

      - name: parsec
        imagePullPolicy: IfNotPresent
        image: spirals/parsec-3.0
        securityContext:
          privileged: true
        args:
        - "-a run"
        - "-i native"
        - "-S parsec"
        - "-n 6"
        - "-p all"
